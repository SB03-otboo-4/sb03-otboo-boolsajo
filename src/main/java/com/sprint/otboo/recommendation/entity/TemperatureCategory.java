package com.sprint.otboo.recommendation.entity;

/**
 * 온도 범주(Temperature Category)를 나타내는 열거형(enum).
 * <p>
 * LOW  - 낮은 온도 / Low<br>
 * HIGH - 높은 온도 / High
 */
public enum TemperatureCategory {
    LOW,
    HIGH
}
